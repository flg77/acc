{
  "_comment": "AUTHORED, NOT CAPTURED. No ACC host runs Ollama as of 2026-08-26, so this is written from the documented /api/show shape rather than taken from a live server. It is named .AUTHORED. so nobody mistakes it for evidence. Replace with a real capture the moment an Ollama host exists; the parser is written to degrade to unknown on every field, so being wrong here costs a report line rather than a bad budget.",
  "license": "Apache License Version 2.0",
  "modelfile": "# Modelfile generated by \"ollama show\"\nFROM qwen2.5:14b",
  "parameters": "stop                           \"<|im_start|>\"\nstop                           \"<|im_end|>\"\nnum_ctx                        8192",
  "template": "{{ .System }}{{ .Prompt }}",
  "details": {
    "parent_model": "",
    "format": "gguf",
    "family": "qwen2",
    "parameter_size": "14.8B",
    "quantization_level": "Q4_K_M"
  },
  "model_info": {
    "general.architecture": "qwen2",
    "general.file_type": 15,
    "general.parameter_count": 14770033664,
    "qwen2.attention.head_count": 40,
    "qwen2.block_count": 48,
    "qwen2.context_length": 32768,
    "qwen2.embedding_length": 5120,
    "qwen2.rope.freq_base": 1000000,
    "qwen2.rope.scaling.type": "yarn",
    "qwen2.rope.scaling.factor": 4.0,
    "qwen2.rope.scaling.original_context_length": 32768,
    "tokenizer.ggml.model": "gpt2"
  }
}
