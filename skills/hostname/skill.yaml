purpose: Report the local host name (cross-platform).
version: 0.1.0
adapter_class: HostnameSkill
risk_level: LOW
domain_id: xplatform
tags:
- xplatform
input_schema:
  type: object
  properties: {}
  additionalProperties: false
output_schema:
  type: object
  properties:
    hostname:
      type: string
    fqdn:
      type: string
  required:
  - hostname
  - fqdn
  additionalProperties: false
description: Report the local host name (cross-platform).
