purpose: Create a directory tree (mkdir -p / md, cross-platform).
version: 0.1.0
adapter_class: MkdirPSkill
risk_level: MEDIUM
domain_id: xplatform
tags:
- xplatform
input_schema:
  type: object
  properties:
    path:
      type: string
  required:
  - path
  additionalProperties: false
output_schema:
  type: object
  properties:
    path:
      type: string
    exists:
      type: boolean
  required:
  - path
  - exists
  additionalProperties: false
description: Create a directory tree (mkdir -p / md, cross-platform).
