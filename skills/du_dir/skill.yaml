purpose: Recursive directory size (cross-platform).
version: 0.1.0
adapter_class: DuDirSkill
risk_level: LOW
domain_id: xplatform
tags:
- xplatform
input_schema:
  type: object
  properties:
    path:
      type: string
  required:
  - path
  additionalProperties: false
output_schema:
  type: object
  properties:
    path:
      type: string
    bytes:
      type: integer
    files:
      type: integer
  required:
  - path
  - bytes
  - files
  additionalProperties: false
description: Recursive directory size (cross-platform).
