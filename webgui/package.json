{
  "name": "acc-webgui",
  "version": "0.1.0",
  "private": true,
  "description": "Optional web frontend for ACC — feature parity with acc-tui plus enhanced tracing (proposal acc-webgui).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.30.4",
    "reactflow": "^11.11.4"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5.5.4",
    "vite": "^7.1.0"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  }
}
