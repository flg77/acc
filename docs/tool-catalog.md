<!-- GENERATED by tools/gen_tool_catalog.py — do not edit by hand.
     Regenerate:  python -m tools.gen_tool_catalog
     CI guard:    python -m tools.gen_tool_catalog --check -->

# Model-facing capability catalog

Every capability a model running in this repo's configuration can be offered. Generated by **booting** the skill and MCP registries — importing adapters, deep-merging `_base` defaults and validating the Pydantic manifests — not by parsing source, because what a registry loads and what a YAML file appears to say are different things.

A capability the model can invoke that appears in no inventory is an ungoverned capability: no category assigned, absent from gap scans, missing from the signed AgentBOM.

| Skills | MCP servers | MCP servers with an unbounded tool surface |
|---|---|---|
| 55 | 12 | **5** |

## What the model is actually told

ACC advertises capabilities in the system prompt as `- <id>: <purpose>` plus the marker grammar (`[SKILL: <id> {json}]`, `[MCP: <server>.<tool> {json}]`), gated by each role's `default_skills` / `default_mcps`.

**The input schema is enforced at invoke but never advertised.** The model is told a capability exists and what it is for, then has to infer the argument shape. The `Enforced input` column below is what the registry validates against — it is not what the model sees.

## ⚠ Unbounded tool surfaces

An MCP manifest with an empty `allowed_tools` list means *allow every tool the server advertises*. The advertised set is owned by the upstream: it can change when the upstream updates, with no ACC code change, no review, and no diff — the agent's capability surface grows silently between two runs. Raw upstream JSON Schema also enters the model-visible corpus unreviewed.

An unbounded tool surface is a governance gap, and these are the servers it applies to:

- **`arxiv`** — arxiv read-only — search papers, fetch metadata + PDFs. (risk `LOW`, transport `stdio`)
- **`rss_fetch`** — RSS / Atom feed read — fetch + parse feed entries. (risk `LOW`, transport `stdio`)
- **`semantic_scholar`** — Semantic Scholar read-only — paper search + citation graph. (risk `LOW`, transport `stdio`)
- **`web_archive`** — Wayback Machine — fetch archived URL snapshots by timestamp. (risk `LOW`, transport `stdio`)
- **`wikipedia`** — Wikipedia read-only — search articles, fetch summaries + content. (risk `LOW`, transport `stdio`)

## Skills

| Skill | Purpose (model-visible) | Risk | Requires actions | Enforced input | Adapter |
|---|---|---|---|---|---|
| `catalog_query` | Read-only catalog awareness for the assistant — enumerate every role the ecosystem provides (installed in-tree, installed-from-package, and available-but-uninstalled) so routing decisions cite real catalog evidence. | `LOW` | — | `{name_filter: string?, running_roles: array?}` | `adapter.CatalogQuerySkill` |
| `citation_tracker` | Citation tracker — records URL → claim → confidence triples. | `LOW` | — | `{text: string}` | `adapter.StubResearchSkill` |
| `code_generation` | Code-generation marker — round-trips the LLM's emitted code verbatim. | `LOW` | — | `{text: string}` | `adapter.StubCodingSkill` |
| `code_review` | Code-review marker — round-trips the LLM's review verbatim. | `LOW` | — | `{text: string}` | `adapter.StubCodingSkill` |
| `competitor_profile` | Competitor-profile marker — round-trips a vendor card. | `LOW` | — | `{text: string}` | `adapter.StubResearchSkill` |
| `count_lines` | Count lines in a text file (wc -l equivalent, cross-platform). | `LOW` | — | `{path: string}` | `adapter.CountLinesSkill` |
| `critic_verdict` | Critic verdict — round-trips PASS/NEEDS_REVISE/FAIL with critique text. | `LOW` | — | `{text: string}` | `adapter.StubResearchSkill` |
| `date_now` | Report the current UTC + local time as ISO-8601 (cross-platform). | `LOW` | — | `object` | `adapter.DateNowSkill` |
| `dependency_audit` | Dependency-audit marker — round-trips CVE + license findings. | `LOW` | — | `{text: string}` | `adapter.StubCodingSkill` |
| `disk_free` | Report disk usage (total, used, free) for a path. | `LOW` | — | `{path: string?}` | `adapter.DiskFreeSkill` |
| `doc_ingest` | Ingest a document into the collective's RAG store (chunk + embed + store). | `MEDIUM` | — | `{title: string, text: string, source: string?, tags: array?}` | `adapter.DocIngestSkill` |
| `doc_retrieve` | Retrieve the most relevant document chunks for a query (governed, collective-scoped). | `LOW` | — | `{query: string, top_k: integer?, doc_id: string?}` | `adapter.DocRetrieveSkill` |
| `du_dir` | Recursive directory size (cross-platform). | `LOW` | — | `{path: string}` | `adapter.DuDirSkill` |
| `echo` | Round-trip the supplied text — CI smoke test. | `LOW` | — | `{text: string}` | `adapter.EchoSkill` |
| `env_get` | Read an allowlisted environment variable. | `LOW` | — | `{name: string}` | `adapter.EnvGetSkill` |
| `find_files` | Find files by glob under a root, bounded depth + count. | `LOW` | — | `{root: string, pattern: string?, max_results: integer?}` | `adapter.FindFilesSkill` |
| `fs_read` | Read a UTF-8 text file from the trusted workspace. | `MEDIUM` | — | `{path: string, max_bytes: integer?}` | `adapter.FsReadSkill` |
| `fs_write` | Write a UTF-8 text file into the trusted workspace. | `HIGH` | — | `{path: string, content: string, mkdirs: boolean?}` | `adapter.FsWriteSkill` |
| `git_log_recent` | Run `git log -n N --oneline` in a workspace directory. | `MEDIUM` | — | `{cwd: string, limit: integer?}` | `adapter.GitLogRecentSkill` |
| `git_status` | Run `git status --short` in a workspace directory. | `MEDIUM` | — | `{cwd: string}` | `adapter.GitStatusSkill` |
| `grep_text` | Search for a literal pattern in a file (line-by-line). | `LOW` | — | `{path: string, pattern: string, max_matches: integer?}` | `adapter.GrepTextSkill` |
| `head_text` | Read the first N lines of a UTF-8 text file (cross-platform). | `LOW` | — | `{path: string, lines: integer?}` | `adapter.HeadTextSkill` |
| `hostname` | Report the local host name (cross-platform). | `LOW` | — | `object` | `adapter.HostnameSkill` |
| `http_get` | HTTP GET a URL (cross-platform via httpx; MEDIUM risk). | `MEDIUM` | `use_external_api` | `{url: string, timeout_s: number?, max_bytes: integer?, headers: object?}` | `adapter.HttpGetSkill` |
| `ls_dir` | List entries of a directory (non-recursive, bounded). | `LOW` | — | `{path: string, max_entries: integer?}` | `adapter.LsDirSkill` |
| `market_sizer` | Market-size marker — round-trips TAM/SAM/SOM estimates. | `LOW` | — | `{text: string}` | `adapter.StubResearchSkill` |
| `mattermost_post` | Create a post in a Mattermost channel via the REST API /api/v4/posts. | `MEDIUM` | `send_message` | `{channel_id: string, text: string}` | `adapter.MattermostPostSkill` |
| `mkdir_p` | Create a directory tree (mkdir -p / md, cross-platform). | `MEDIUM` | — | `{path: string}` | `adapter.MkdirPSkill` |
| `okf` | Produce & self-check OKF-conformant knowledge (pure text ops). | `LOW` | — | `{op: string, frontmatter: object?, body: string?, text: string?, path_hint: string?}` | `adapter.OkfSkill` |
| `okf_transform` | Read, validate, query, write & convert OKF bundles in the workspace. | `HIGH` | — | `{op: string, path: string?, rel_path: string?, frontmatter: object?, body: string?, type: string?, tags: array?, limit: integer?, vault: string?, dest: string?, now: string?}` | `adapter.OkfTransformSkill` |
| `plan_outline` | Plan-outline marker — round-trips the LLM's research outline. | `LOW` | — | `{text: string}` | `adapter.StubResearchSkill` |
| `pwd` | Report the agent's current working directory. | `LOW` | — | `object` | `adapter.PwdSkill` |
| `python_exec` | Run a Python snippet in an isolated subprocess and return stdout/stderr. | `HIGH` | `execute_shell` | `{code: string, cwd: string?, timeout_s: integer?}` | `adapter.PythonExecSkill` |
| `read_json` | Read + parse a JSON file (cross-platform). | `LOW` | — | `{path: string, max_bytes: integer?}` | `adapter.ReadJsonSkill` |
| `read_text_head` | Read the first N bytes of a UTF-8 text file. | `LOW` | — | `{path: string, max_bytes: integer?}` | `adapter.ReadTextHeadSkill` |
| `read_text_tail` | Read the last N bytes of a UTF-8 text file. | `LOW` | — | `{path: string, max_bytes: integer?}` | `adapter.ReadTextTailSkill` |
| `release_pipe` | Plan the ACC release pipeline for a role/skill/pack (review→build→sign→publish→catalog→promote). | `LOW` | — | `{kind: string?, name: string, version: string?, pack: string?, in_tree: boolean?}` | `adapter.ReleasePipeSkill` |
| `report_drafter` | Report-drafter marker — round-trips a markdown section body. | `LOW` | — | `{text: string}` | `adapter.StubResearchSkill` |
| `role_author` | Scaffold a new ACC role (role.yaml + role.md) — draft for review, then write. | `MEDIUM` | — | `{name: string, purpose: string, persona: string?, task_types: array?, domain_id: string?, seed_context: string?, reasoning_trace: boolean?, allowed_skills: array?, allowed_mcps: array?, max_skill_risk_level: string?, description: string?, version: string?, mode: string?, base_dir: string?}` | `adapter.RoleAuthorSkill` |
| `security_scan` | Security-scan marker — round-trips findings emitted by the LLM. | `LOW` | — | `{text: string}` | `adapter.StubCodingSkill` |
| `shell_exec` | Run a process (argv list, no shell expansion) and return stdout/stderr. | `HIGH` | `execute_shell` | `{argv: array?, cmd: string?, cwd: string?, timeout_s: integer?}` | `adapter.ShellExecSkill` |
| `skill_author` | Scaffold a new ACC skill (skill.yaml + adapter.py) — draft for review, then write. | `MEDIUM` | — | `{name: string, purpose: string, risk_level: string?, domain_id: string?, tags: array?, description: string?, input_schema: object?, output_schema: object?, invoke_body: string?, mode: string?, base_dir: string?}` | `adapter.SkillAuthorSkill` |
| `slack_post` | Post a message to a Slack channel via the Web API chat.postMessage. | `MEDIUM` | `send_message` | `{channel: string, text: string}` | `adapter.SlackPostSkill` |
| `speech_synthesize` | Synthesize speech audio (WAV) from text (Piper, fully on-device). | `MEDIUM` | — | `{text: string, out_path: string?}` | `adapter.SpeechSynthesizeSkill` |
| `speech_transcribe` | Transcribe a local audio file to text (Whisper, fully on-device). | `MEDIUM` | — | `{audio_path: string, language: string?}` | `adapter.SpeechTranscribeSkill` |
| `ssh_exec` | Run a command on a remote host via SSH (argv only, key auth, allowlisted hosts). | `HIGH` | `execute_ssh` | `{host: string, user: string?, argv: array?, cmd: string?, port: integer?, identity_file: string?, timeout_s: integer?}` | `adapter.SshExecSkill` |
| `stat_path` | Stat a filesystem path (size, mtime, type). | `LOW` | — | `{path: string}` | `adapter.StatPathSkill` |
| `tail_text` | Read the last N lines of a UTF-8 text file (cross-platform). | `LOW` | — | `{path: string, lines: integer?}` | `adapter.TailTextSkill` |
| `telegram_send` | Send a text message to a Telegram chat via the official Bot API. | `MEDIUM` | `send_message` | `{chat_id: any, text: string}` | `adapter.TelegramSendSkill` |
| `test_execution` | Test-execution marker — surfaces a synthetic pass/fail summary. | `LOW` | — | `{text: string}` | `adapter.StubCodingSkill` |
| `test_generation` | Test-generation marker — round-trips the LLM's emitted pytest module. | `LOW` | — | `{text: string}` | `adapter.StubCodingSkill` |
| `touch_file` | Create an empty file or update mtime (touch, cross-platform). | `MEDIUM` | — | `{path: string}` | `adapter.TouchFileSkill` |
| `uname_info` | Report OS / architecture / kernel (cross-platform via platform module). | `LOW` | — | `object` | `adapter.UnameInfoSkill` |
| `which_cmd` | Locate an executable on PATH (shutil.which). | `LOW` | — | `{name: string}` | `adapter.WhichCmdSkill` |
| `whoami` | Report the current process user (cross-platform). | `LOW` | — | `object` | `adapter.WhoamiSkill` |

## MCP servers

| Server | Purpose (model-visible) | Risk | Transport | Tool surface | Requires actions |
|---|---|---|---|---|---|
| `arxiv` | arxiv read-only — search papers, fetch metadata + PDFs. | `LOW` | `stdio` | **unbounded** _(whatever the server advertises)_ | — |
| `echo_server` | Smoke-test MCP server: round-trip a single 'echo' tool. | `LOW` | `http` | `echo` | — |
| `github_api` | GitHub REST API — repo browse, issues, PRs, code search. | `MEDIUM` | `stdio` | `search_repositories`, `search_issues`, `search_pull_requests`, `search_code`, `get_file_contents`, `list_commits`, `get_issue`, `get_pull_request`, `list_issues`, `list_pull_requests`<br>denied: `create_or_update_file`, `delete_file`, `create_issue`, `create_pull_request` | `use_external_api` |
| `google_workspace` | Read Google Workspace (Calendar, Gmail, Drive, Sheets) on the operator's behalf via a consumed MCP server, authenticated per-operator with brokered OAuth 2.1. Read-first: mutating tools are denied in this slice. | `MEDIUM` | `http` | `calendar_list_events`, `calendar_get_event`, `gmail_search`, `gmail_get_message`, `drive_list_files`, `drive_get_file`, `sheets_read_range`<br>denied: `gmail_send`, `drive_delete`, `sheets_write_range`, `calendar_delete_event` | — |
| `rss_fetch` | RSS / Atom feed read — fetch + parse feed entries. | `LOW` | `stdio` | **unbounded** _(whatever the server advertises)_ | — |
| `semantic_scholar` | Semantic Scholar read-only — paper search + citation graph. | `LOW` | `stdio` | **unbounded** _(whatever the server advertises)_ | — |
| `signal` | Send and receive Signal messages via a self-hosted signal-cli-rest-api. The most sovereign messenger target (E2E encryption, no third-party server). | `MEDIUM` | `http` | `send`, `receive` | `send_message` |
| `web_archive` | Wayback Machine — fetch archived URL snapshots by timestamp. | `LOW` | `stdio` | **unbounded** _(whatever the server advertises)_ | — |
| `web_browser_harness` | Drive a real Chromium browser to perform a research task. Primary research tool for ACC autoresearcher personas; the underlying LLM client follows the harness's chain-of-action navigation pattern. | `HIGH` | `http` | `browse` | — |
| `web_fetch` | Retrieve a URL and return its body as markdown-ish text plus a paywalled flag. Default citation-grounding tool for ACC autoresearcher personas. | `MEDIUM` | `http` | `fetch` | — |
| `web_search_brave` | Web search via Brave Search API. Default fallback for research personas when the heavier browser_harness MCP is overkill. | `MEDIUM` | `http` | `search` | — |
| `wikipedia` | Wikipedia read-only — search articles, fetch summaries + content. | `LOW` | `stdio` | **unbounded** _(whatever the server advertises)_ | — |

## Completeness

The generator globs `skills/*/` and `mcps/*/` and fails if anything on disk did not reach the registry. This matters because `load_from()` deliberately logs-and-drops a capability whose adapter will not import — correct at runtime (one bad skill must not knock out the healthy ones), dangerous at inventory time, because the capability leaves the model-facing surface without saying so.

Enforced by `tests/test_tool_catalog.py`. A new skill or MCP cannot be silently undocumented.

