# Staging dir for a local file: catalog (acc-deploy.sh flavour --local-catalog).
# Built .accpkg/.sha256/catalogs.yaml land here at build time and are gitignored;
# this .gitkeep keeps the dir present so Containerfile.flavour's COPY has a source.
