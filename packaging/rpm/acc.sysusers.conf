# The unprivileged runtime user for the ACC stack (IN-06).  It never gains
# root: no sudoers entry, no capabilities; rootless podman under its own
# sub-uid range (added in %pre).
u acc - "ACC runtime" /var/lib/acc /sbin/nologin
